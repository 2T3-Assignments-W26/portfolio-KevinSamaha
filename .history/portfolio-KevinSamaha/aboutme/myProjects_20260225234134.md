# Programing projects Year one #

## Rainfall Statistics ##

**Description:** Had to design a program that lets the user enter the total rainfall for each month of the year into an
array. Validating the input, displaying an appropriate error message if or when the
user enters invalid input. Ihad to also make a function to calculate and display the total
rainfall for the year, another function to compute and return the 
average monthly rainfall, and another function to display the
months with the highest and lowest amounts of rain.

**Coded** in _C#_ in Vertual studio
```c#
// user inputs the month

    static Int32[] WeatherForEachMonth(Int32[] UserWeatherInput)
    {
        string[] Month = DateTimeFormatInfo.CurrentInfo.MonthNames;
        Console.WriteLine("what is the total rainfall for each month");
        for (int i = 0; i < 12; i++)
        {
            Console.WriteLine("Total rain fall in " + Month[i]);
            while (true)
            {
                //checks if input is a int if it is the function will return to main if not user will retry
                if (int.TryParse(Console.ReadLine(), out UserWeatherInput[i]))
                {
                    break;
                }
                Console.WriteLine("Try again only numbers allowed");
            }            
        }
        return UserWeatherInput;
    }
```
### Photos ###
![alt text](project1_photo1.png)
![alt text](project1_photo2.png)
![alt text](project1_photo3.png)

**What I learned:** how to use _.Tryparse_ in C# it works by only accepting the value attached to it for example if it's an _int_ it'll only accepts _int_ if not it'll rejects it.


## Final project video game ##

**Description:** In this assignment I had to make a video game I maded space invaders making the character move up and down shoot the aliens and have alives system.

**Coded** in _C#_ in Vertual studio

```c#
 //draws Alien
 static void DrawAlien()
 {
     for (Int32 a = 0; a < AlienCount; a++)
     {
         Console.SetCursorPosition(AlienX[a], AlienY[a]);
         Console.Write(Alien);
     }
 }
```
### Photos ###

![alt text](project2_photo1.png)
![alt text](project2_photo2.png)
![alt text](project2_photo3.png)
![alt text](project2_photo4.png)
![alt text](project2_photo5.png)
![alt text](project2_photo6.png)
![alt text](project2_photo7.png)

**What I learned:** learned how to use arrays in this example we are filling an int32 array by using a for loop. When it add's a one it fills the corresponding array. For example it adds a 1 is for array 1, 2 is for array 2, etc. this example uses these arrays to draw the corrdinates and than draw an alien thats a *.
